# ==========================================
# INCORPORATOR DEV ENVIRONMENT
# ==========================================
# This installs the package in editable mode and automatically pulls
# all development dependencies (pytest, mypy, ruff, etc.) from pyproject.toml.

-e .[dev]