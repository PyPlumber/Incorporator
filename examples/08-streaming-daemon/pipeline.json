{
  "_comment_": "T8 canonical use: chunking-mode CoinGecko full-catalogue drain. Matches streaming_daemon.py's default chunking_demo() (no page cap -- see README addendum re: wall-clock). Run from this directory: incorporator validate pipeline.json && incorporator stream pipeline.json --logs",

  "incorp_params": {
    "inc_url": "https://api.coingecko.com/api/v3/coins/markets",
    "params": {"vs_currency": "usd", "per_page": 250},
    "inc_code": "id",
    "inc_name": "name",
    "inc_page": "PageNumberPaginator(page_param='page')",
    "excl_lst": ["image"]
  },

  "refresh_params": null,
  "export_params": {
    "file_path": "out/coins_full.ndjson",
    "if_exists": "append"
  },
  "stateful_polling": false
}
