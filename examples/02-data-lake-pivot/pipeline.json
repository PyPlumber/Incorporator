{
  "_comment_": "T2 data-lake pivot: JSON -> SQLite via stream(). Run from this directory: incorporator validate pipeline.json && incorporator stream pipeline.json",

  "incorp_params": {
    "inc_url": "https://jsonplaceholder.typicode.com/users",
    "inc_code": "id",
    "inc_name": "name"
  },

  "export_params": {
    "file_path": "out/users_warehouse.db",
    "sql_table": "employees"
  }
}
